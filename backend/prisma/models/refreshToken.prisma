model RefreshToken {
  id        String   @id @default(uuid())
  userId    String
  creator   User     @relation(fields: [userId], references: [id])
  token     String   @unique
  expiresAt DateTime
}
